{
  "name": "algorithmic-madness",
  "module": "src/index.ts",
  "type": "module",
  "scripts": {
    "prepare": "husky install",
    "apply-ci": "biome check --apply-unsafe . && biome format --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "^1.4.0",
    "@commitlint/cli": "^18.4.3",
    "@commitlint/config-conventional": "^18.4.3",
    "bun-types": "latest",
    "husky": "^8.0.3"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
